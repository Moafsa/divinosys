# WuzAPI Frontend Configuration
# Este arquivo define variáveis de ambiente para o frontend React do WuzAPI

# URL do backend WuzAPI - mesma porta que o frontend!
# Como o WuzAPI serve frontend+API na MESMA porta 8080, use a URL base
REACT_APP_API_URL=${WUZAPI_PUBLIC_URL:-http://localhost:8080}

# Timeout das requisições (em milissegundos)
REACT_APP_TIMEOUT=30000

# Ambiente (development, production)
NODE_ENV=production
